{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "node build.js --dist --edition=launch && node build.js && cp HolaRuta.html dist/HolaRuta.html",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/v1/:path*", "destination": "/api/v1?path=:path*" }
  ],
  "crons": [
    { "path": "/api/cron/purge-events", "schedule": "0 3 * * *" }
  ],
  "functions": {
    "api/**/*.js": { "memory": 256, "maxDuration": 15 }
  }
}
