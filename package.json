{
  "name": "holaruta",
  "version": "1.109.0",
  "private": true,
  "description": "HolaRuta – Dein Reise-Spanisch für echte Situationen. Vanilla-JS-Lernkarten-PWA (keine Runtime-Dependencies).",
  "scripts": {
    "test": "node --test",
    "build": "node build.js",
    "dist": "node build.js --dist",
    "e2e": "node scripts/e2e-modo-profe.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
