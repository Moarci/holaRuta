# Dependencies
node_modules/

# Build / temp artifacts
tmp_*.js
tmp_*.json
.playwright-mcp/

# E2E-Ausgabe (Screenshots/CSV aus scripts/e2e-modo-profe.mjs)
scripts/.e2e-out/

# Generierter Single-File-Build (Quelle = Module; via `node build.js`)
HolaRuta.html

# Edition-Builds (reproduzierbar via `node build.js --edition=<id>`)
HolaRuta-*.html

# Minifizierter Multi-File-Deploy-Build (reproduzierbar via `node build.js --dist`)
dist/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Local Claude settings (keep shared config, ignore local overrides)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Stray screenshots
info-accordion.png
